# Flujo de trabajo

## Coding

R es una buena calculadora:

```{r}
1 / 200 * 30
(59 + 73 + 2) / 3
sin(pi / 2)
```

Asigna a variables con `<-`:

```{r}
x <- 3 * 4
```

Forma general de una asignaci??n:

```{r eval = FALSE}
object_name <- value
```

Tip: usa Alt + - para introducir la flecha en **RStudio**

## Nombres de variable

Los nombres de los objetos en R empiezan con una letter y est??n formados por letras (preferiblemente en ascii o parte del conjunto base de UTF7), n??meros, gui??n bajo y ??punto! Algunos ejemplos son:
```{r, eval = FALSE}
i_use_snake_case
otherPeopleUseCamelCase
some.people.use.periods
And_aFew.People_RENOUNCEconvention
```

La consola interactiva de R imprime el valor devuelto por la ??ltima expresi??n ejecutada en R:

```{r}
x
```

Usando la tecla de tabulaci??n, podemos completar expresiones largas:

```{r}
this_is_a_really_long_name <- 2.5
```

Usa las flechas arriba y abajo (???/??? ) para navegar por el historial.

Crea una nueva variable:

```{r}
r_rocks <- 2 ^ 3
```

Checa su valor. ??Qu?? pas???

```{r, eval = FALSE}
r_rock
#> Error: object 'r_rock' not found
R_rocks
#> Error: object 'R_rocks' not found
```


## Funciones

Hay MUCHAS funciones en R. Todas se ejecutan de la siguiente manera:

```{r eval = FALSE}
function_name(arg1 = val1, arg2 = val2, ...)
```

Hay una funci??n llamada 'seq()' y otra llamada 'c()' que se utilizan mucho en R. ??Para qu?? servir??n?

```{r}
seq(1, 10)
```

Aqu?? crearemos una cadena de texto:

```
x <- "hello world
```

```
> x <- "hello
+
```

??Qu?? pas?? ahora?

La asignaci??n no imprime nada, pues no regresa un valor, s??lo almacena el valor en una variable.

As?? que podemos hacer lo siguiente,

```{r}
y <- seq(1, 10, length.out = 5)
y
```

o a??n mejor:

```{r}
(y <- seq(1, 10, length.out = 5))
```

??Qu?? har?? la funci??n 'ls()'?

```{r, echo = FALSE, out.width = NULL}
knitr::include_graphics("screenshots/rstudio-env.png")
```

## Ejercicios:

1.  ??Por qu?? no funciona este c??digo?

    ```{r, error = TRUE}
    my_variable <- 10
    my_var??able
    ```

1.  Tweak each of the following R commands so that they run correctly:

    ```{r, eval = FALSE}
    library(tidyverse)

    ggplot(dota = mpg) + 
      geom_point(mapping = aes(x = displ, y = hwy))
    
    fliter(mpg, cyl = 8)
    filter(diamond, carat > 3)
    ```

??Ya viste los errores? ??Qu?? har?? la funci??n 'filter()'?
    
1.  Intenta usar Alt + Shift + K en RStudio, ??qu?? pas???

